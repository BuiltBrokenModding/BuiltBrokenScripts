if (project.hasProperty("bambooshortPlanName") && project.getProperty("local") == "BM-Dev""){
    group = "dev.${org}.${groupid}"
    archivesBaseName = "${archivebase}"
} else {
    group = "com.${org}.${groupid}"
    archivesBaseName = "${archivebase}"
}
