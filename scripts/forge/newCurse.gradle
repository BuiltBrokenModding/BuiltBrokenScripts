def curseRelationsVE = {
    optionalLibrary 'cofhcore'
    optionalLibrary 'thermal-foundation'
    optionalLibrary 'thermalexpansion'
    optionalLibrary 'notenoughitems'
    optionalLibrary 'waila'
    optionalLibrary 'opencomputers'
}

def curseRelations = {
    optionalLibrary 'cofhcore'
    optionalLibrary 'thermal-foundation'
    optionalLibrary 'thermalexpansion'
    requiredLibrary 'voltz-engine'
    optionalLibrary 'notenoughitems'
    optionalLibrary 'waila'
    optionalLibrary 'opencomputers'
}

def curseRelationsNV = {
    optionalLibrary 'cofhcore'
    optionalLibrary 'thermal-foundation'
    optionalLibrary 'thermalexpansion'
    optionalLibrary 'voltz-engine'
    optionalLibrary 'notenoughitems'
    optionalLibrary 'waila'
    optionalLibrary 'opencomputers'
}

curseforge {
  if (project.hasProperty("bbmcurse")) {
        apiKey = project.bbmcurse
    }
    else {
    apiKey = ""
    }
  project {
  // VoltzEngine
    id = "227048"
    relations curseRelationsVE
    changelog = file('changelog.html')
    releaseType = "alpha"
    addGameVersion "1.7.10"
    mainArtifact(jar) {
      displayName = "VoltzEngine ${project.version}-UNSTABLE build ${project.getBuildNumber}() for 1.7.10"
    }
    addArtifact(deobfJar)
      displayName = "VoltzEngine deobf ${project.version}}-UNSTABLE build $${project.getBuildNumber}() for 1.7.10"
  }
  project {
  //ICBM
    id = '225502'
    relations curseRelations
    changelog = file('changelog.html')
    releaseType = "alpha"
    addGameVersion "1.7.10"
    mainArtifact(jar) {
      displayName = "ICBM ${project.version}-UNSTABLE build ${project.getBuildNumber()} for 1.7.10"
    }
    addArtifact(deobfJar){
        displayName = "ICBM ${project.version}-UNSTABLE build ${project.getBuildNumber()} for 1.7.10"
    }
  }
  project {
  //NotEnoughKeys
    id = '224614'
    //relations curseRelations
    changelog = file('changelog.html')
    releaseType = "release"
    addGameVersion "1.7.10"
    mainArtifact(jar) {
      displayName = "NEK version: ${project.version}"
    }
    addArtifact (deobfJar)
           displayName = "NEK version deobf: ${project.version}"
  }
  project{
  //MiliraryBaseDecor
    id = '244730'
    relations curseRelations
    releaseType = "alpha"
    changelog = file('changelog.html')
    addGameVersion "1.7.10"
    mainArtifact(jar) {
      displayName = "MilitaryBaseDecor ${project.version}-UNSTABLE build ${project.getBuildNumber}() for 1.7.10"
    }
    addArtifact(deobfJar){
      displayName = "MilitaryBaseDecor ${project.version}-UNSTABLE-Deobf build ${project.getBuildNumber}() for 1.7.10"
    }

  }
  project{
  //Basic Industry
    id = '256398'
    relations curseRelations
    releaseType = "alpha"
    addGameVersion "1.7.10"
    changelog = file('changelog.html')
    mainArtifact(jar) {
      displayName = "Basic Industry ${project.version}-UNSTABLE build ${project.getBuildNumber}() for 1.7.10"
    }
    addArtifact(deobfJar){
      displayName = "Basic Industry ${project.version}-UNSTABLE-deobf build ${project.getBuildNumber}() for 1.7.10"
    }
  }
  project{
  //MFFS
    id = '238546'
    relations curseRelations
    releaseType = "alpha"
    addGameVersion "1.7.10"
    changelog = file('changelog.html')
    mainArtifact(jar) {
      displayName = "MFFS ${project.version}-UNSTABLE build ${project.getBuildNumber}() for 1.7.10"
    }
    addArtifact(deobfJar){
      displayName = "MFFS ${project.version}-UNSTABLE-deobf build ${project.getBuildNumber}() for 1.7.10"
    }
  }
  project{
  //SBM Helm Bucket
    id = '256467'
    relations curseRelationsNV
    releaseType = "alpha"
    addGameVersion "1.7.10"
    changelog = file('changelog.html')
    mainArtifact(jar) {
      displayName = "SBM-HelmBucket ${project.version}-UNSTABLE build ${project.getBuildNumber}() for 1.7.10"
    }
    addArtifact(deobfJar){
      displayName = "SBM-HelmBucket ${project.version}-UNSTABLE-deobf build ${project.getBuildNumber}() for 1.7.10"
    }
  }
  project{
  //SBM Bagable Plants
    id = '256548'
    relations curseRelationsNV
    releaseType = "alpha"
    addGameVersion "1.7.10"
    changelog = file('changelog.html')
    mainArtifact(jar) {
      displayName = "SBM-BagablePlants ${project.version}-UNSTABLE build ${project.getBuildNumber}() for 1.7.10"
    }
    addArtifact(deobfJar){
      displayName = "SBM-BagablePlants ${project.version}-UNSTABLE-deobf build ${project.getBuildNumber}() for 1.7.10"
    }
  }
  project{
  //SBM Cacti Bucket
    id = '256605'
    relations curseRelationsNV
    releaseType = "alpha"
    addGameVersion "1.7.10"
    changelog = file('changelog.html')
    mainArtifact(jar) {
      displayName = "SBM-CactiBucket ${project.version}-UNSTABLE build ${project.getBuildNumber}() for 1.7.10"
    }
    addArtifact(deobfJar){
      displayName = "SBM-CactiBucket ${project.version}-UNSTABLE-deobf build ${project.getBuildNumber}() for 1.7.10"
    }
  }
  project{
  //Assembly Line
    id = '235544'
    relations curseRelations
    releaseType = "alpha"
    addGameVersion "1.7.10"
    changelog = file('changelog.html')
    mainArtifact(jar) {
      displayName = "Assembly Line ${project.version}-UNSTABLE build ${project.getBuildNumber}() for 1.7.10"
    }
    addArtifact(deobfJar){
      displayName = "Assembly Line ${project.version}-UNSTABLE-deobf build ${project.getBuildNumber}() for 1.7.10"
    }
  }
  project{
  //SBM Magic Mirror
    id = '250881'
    relations curseRelationsNV
    releaseType = "alpha"
    addGameVersion "1.7.10"
    changelog = file('changelog.html')
    mainArtifact(jar) {
      displayName = "SBM-Magic-Mirror ${project.version}-UNSTABLE build ${project.getBuildNumber}() for 1.7.10"
    }
    addArtifact(deobfJar){
      displayName = "SBM-Magic-Mirror ${project.version}-UNSTABLE-deobf build ${project.getBuildNumber}() for 1.7.10"
    }
  }
  project{
  //SBM Wooden Shears
    id = '250880'
    relations curseRelationsNV
    releaseType = "alpha"
    addGameVersion "1.7.10"
    changelog = file('changelog.html')
    mainArtifact(jar) {
      displayName = "SBM-WoodenShears ${project.version}-UNSTABLE build ${project.getBuildNumber}() for 1.7.10"
    }
    addArtifact(deobfJar){
      displayName = "SBM-WoodenShears ${project.version}-UNSTABLE-deobf build ${project.getBuildNumber}() for 1.7.10"
    }
  }
  project{
  //SBM CardboardBoxes
    id = '248343'
    relations curseRelationsNV
    releaseType = "alpha"
    addGameVersion "1.7.10"
    changelog = file('changelog.html')
    mainArtifact(jar) {
      displayName = "SBM-CardboardBoxes ${project.version}-UNSTABLE build ${project.getBuildNumber}() for 1.7.10"
    }
    addArtifact(deobfJar){
      displayName = "SBM-CardboardBoxes ${project.version}-UNSTABLE-deobf build ${project.getBuildNumber}() for 1.7.10"
    }
  }
  project{
  //Progressive World Cleanup
    id = '246228'
    relations curseRelationsNV
    releaseType = "alpha"
    addGameVersion "1.7.10"
    changelog = file('changelog.html')
    mainArtifact(jar) {
      displayName = "ProgressiveWorldCleanup ${project.version}-UNSTABLE build ${project.getBuildNumber}() for 1.7.10"
    }
    addArtifact(deobfJar){
      displayName = "ProgressiveWorldCleanup ${project.version}-UNSTABLE-deobf build ${project.getBuildNumber}() for 1.7.10"
    }
  }
  project{
  //SBM Bone Torch
    id = '238695'
    relations curseRelationsNV
    releaseType = "alpha"
    addGameVersion "1.7.10"
    changelog = file('changelog.html')
    mainArtifact(jar) {
      displayName = "SBM-BoneTorch ${project.version}-UNSTABLE build ${project.getBuildNumber}() for 1.7.10"
    }
    addArtifact(deobfJar){
      displayName = "SBM-BoneTorch ${project.version}-UNSTABLE-deobf build ${project.getBuildNumber}() for 1.7.10"
    }
  }
  project{
  //SBM Wooden Buckets
    id = '233189'
    relations curseRelationsNV
    releaseType = "alpha"
    addGameVersion "1.7.10"
    changelog = file('changelog.html')
    mainArtifact(jar) {
      displayName = "SBM-WoodenBucket ${project.version}-UNSTABLE build ${project.getBuildNumber}() for 1.7.10"
    }
    addArtifact(deobfJar){
      displayName = "SBM-WoodenBucket ${project.version}-UNSTABLE-deobf build ${project.getBuildNumber}() for 1.7.10"
    }
  }
  project{
  //SBM Red Cow
    id = '233525'
    relations curseRelationsNV
    releaseType = "alpha"
    addGameVersion "1.7.10"
    changelog = file('changelog.html')
    mainArtifact(jar) {
      displayName = "SBM-RedCow ${project.version}-UNSTABLE build ${project.getBuildNumber}() for 1.7.10"
    }
    addArtifact(deobfJar){
      displayName = "SBM-RedCow ${project.version}-UNSTABLE-deobf build ${project.getBuildNumber}() for 1.7.10"
    }
  }
  project{
  //Addicted To Red
    id = '236575'
    relations curseRelations
    releaseType = "alpha"
    addGameVersion "1.7.10"
    changelog = file('changelog.html')
    mainArtifact(jar) {
      displayName = "AddictedToRed ${project.version}-UNSTABLE build ${project.getBuildNumber}() for 1.7.10"
    }
    addArtifact(deobfJar){
      displayName = "AddictedToRed ${project.version}-UNSTABLE-deobf build ${project.getBuildNumber}() for 1.7.10"
    }
  }
  project{
  //AI Improvements
    id = '233019'
    relations curseRelationsNV
    releaseType = "alpha"
    addGameVersion "1.7.10"
    changelog = file('changelog.html')
    mainArtifact(jar) {
      displayName = "AI Improvements ${project.version}-UNSTABLE build ${project.getBuildNumber}() for 1.7.10"
    }
    addArtifact(deobfJar){
      displayName = "AI Improvements ${project.version}-UNSTABLE-deobf build ${project.getBuildNumber}() for 1.7.10"
    }
  }
  project{
  //External Announcements
    id = '243419'
    relations curseRelationsNV
    releaseType = "alpha"
    addGameVersion "1.7.10"
    changelog = file('changelog.html')
    mainArtifact(jar) {
      displayName = "Announcement-Mod ${project.version}-UNSTABLE build ${project.getBuildNumber}() for 1.7.10"
    }
    addArtifact(deobfJar){
      displayName = "Announcement-Mod ${project.version}-UNSTABLE-deobf build ${project.getBuildNumber}() for 1.7.10"
    }
  }
  project{
  //SBM Colored Chests
    id = '233511'
    relations curseRelationsNV
    releaseType = "alpha"
    addGameVersion "1.7.10"
    changelog = file('changelog.html')
    mainArtifact(jar) {
      displayName = "ColoredChests ${project.version}-UNSTABLE build ${project.getBuildNumber}() for 1.7.10"
    }
    addArtifact(deobfJar){
      displayName = "ColoredChests ${project.version}-UNSTABLE-deobf build ${project.getBuildNumber}() for 1.7.10"
    }
  }
  project{
  //SBM Wooden Rails
    id = '233511'
    relations curseRelationsNV
    releaseType = "alpha"
    addGameVersion "1.7.10"
    changelog = file('changelog.html')
    mainArtifact(jar) {
      displayName = "WoodenRails ${project.version}-UNSTABLE build ${project.getBuildNumber}() for 1.7.10"
    }
    addArtifact(deobfJar){
      displayName = "WoodenRails ${project.version}-UNSTABLE-deobf build ${project.getBuildNumber}() for 1.7.10"
    }
  }
  project{
  //Artillects
    id = '224319'
    relations curseRelations
    releaseType = "alpha"
    addGameVersion "1.7.10"
    changelog = file('changelog.html')
    mainArtifact(jar) {
      displayName = "Artillects ${project.version}-UNSTABLE build ${project.getBuildNumber}() for 1.7.10"
    }
    addArtifact(deobfJar){
      displayName = "Artillects ${project.version}-UNSTABLE-deobf build ${project.getBuildNumber}() for 1.7.10"
    }
  }
  project{
  //ICBM - Classic
    id = '224319'
    relations curseRelations
    releaseType = "alpha"
    addGameVersion "1.7.10"
    changelog = file('changelog.html')
    mainArtifact(jar) {
      displayName = "ICBM - Classic ${project.version}-UNSTABLE build ${project.getBuildNumber}() for 1.7.10"
    }
    addArtifact(deobfJar){
      displayName = "ICBM - Classic ${project.version}-UNSTABLE-deobf build ${project.getBuildNumber}() for 1.7.10"
    }
  }
}
